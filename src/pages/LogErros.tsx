import React,{useState}from'react';
import{Search}from'lucide-react';
import{getSystemErrors}from'../services/adminActions';
export default function LogErros(){const[search,setSearch]=useState(''),logs=getSystemErrors(),visible=logs.filter(log=>!search||[log.operationId,log.module,log.message].some(value=>value.toLowerCase().includes(search.toLowerCase())));return <div className="space-y-4"><h2 className="font-black">Log de Erros do Sistema</h2><label className="relative block"><Search className="absolute left-3 top-2.5 h-4 w-4"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar por Operation ID, módulo ou mensagem" className="w-full rounded border py-2 pl-9 text-xs"/></label>{visible.length===0&&<p className="rounded border bg-white p-6 text-xs">Nenhum erro registrado nesta instalação.</p>}{visible.map(log=><article key={log.operationId} className="rounded border bg-white p-4 text-xs"><b>{log.operationId}</b><p>{log.module} · {new Date(log.occurredAt).toLocaleString('pt-BR')}</p><p className="mt-2">{log.message}</p></article>)}</div>}
