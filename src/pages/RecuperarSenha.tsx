import React,{useState}from'react';import{useAuth}from'../auth/AuthContext';
export default function RecuperarSenha(){const{updatePassword}=useAuth();const[password,setPassword]=useState(''),[message,setMessage]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();try{await updatePassword(password);setMessage('Senha atualizada. Você já pode retornar ao ERP.');}catch(error){setMessage(error instanceof Error?error.message:'Não foi possível atualizar.');}};return <main className="min-h-screen grid place-items-center bg-background p-4"><form onSubmit={submit} className="w-full max-w-sm rounded-xl border bg-white p-7"><h1 className="font-black">Definir nova senha</h1><input required minLength={8} type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-5 w-full border p-3" placeholder="Nova senha"/>{message&&<p className="mt-3 text-xs">{message}</p>}<button className="mt-4 w-full bg-primary-container p-3 font-black">ATUALIZAR SENHA</button></form></main>}
