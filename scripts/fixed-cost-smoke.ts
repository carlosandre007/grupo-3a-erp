const storage=new Map<string,string>();Object.defineProperty(globalThis,'localStorage',{value:{getItem:(key:string)=>storage.get(key)??null,setItem:(key:string,value:string)=>storage.set(key,value),removeItem:(key:string)=>storage.delete(key)}});Object.defineProperty(globalThis,'window',{value:{dispatchEvent:()=>true}});Object.defineProperty(globalThis,'crypto',{value:{randomUUID:()=>`11111111-1111-4111-8111-${String(storage.size).padStart(12,'0')}`}});
const{addFixedCost,getFixedCosts,getTransactions,payFixedCost}=await import('../src/mockData');
const base={description:'Internet',company:'LOC MOTTUS'as const,category:'Internet',value:100,dueDay:31,frequency:'mensal'as const,bankAccountId:undefined,startDate:'2026-02-01',endDate:undefined,active:true,status:'ativo'as const,autoGenerate:true,notes:undefined};
const monthly=addFixedCost(base);if(monthly.nextDueDate!=='2026-02-28'||getTransactions().length!==1||getTransactions()[0].status!=='pendente')throw new Error('Mensal/Fluxo inválido.');payFixedCost(monthly.id);if(getTransactions().filter(tx=>tx.id.includes(monthly.id)).length!==2||getFixedCosts()[0].nextDueDate!=='2026-03-31')throw new Error('Recorrência ou duplicidade inválida.');
addFixedCost({...base,description:'Sem lançamento',autoGenerate:false});if(getTransactions().length!==2)throw new Error('Toggle de geração ignorado.');
addFixedCost({...base,description:'Sem conta',bankAccountId:undefined,dueDay:2,frequency:'semanal'});if(getTransactions().filter(tx=>tx.description==='Sem conta').length!==1)throw new Error('Conta opcional falhou.');
console.log('FIXED_COST=OK MONTH_END=OK RECURRENCE=OK OPTIONAL_BANK=OK AUTO_FLOW=OK DUPLICATE_PROTECTION=OK');
